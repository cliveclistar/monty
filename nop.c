#include "monty.h"
/**
  * the f_nop- nothing
  * the @head: stack head
  * the @counter: line_number
  *Return: no return
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
